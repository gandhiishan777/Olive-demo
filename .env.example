# Olive V0 — environment

# Backend port (default 8787)
PORT=8787

# Supabase Postgres — Settings → Database → Connection string → Session pooler
SUPABASE_DB_URL=

# Restaurant identity (used in order number prefix + system prompt)
RESTAURANT_NAME=Paradise Biryani
RESTAURANT_TIMEZONE=America/Los_Angeles
